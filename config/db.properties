# Database Configuration
# IMPORTANTE: Questo file NON deve essere committato in versioning!
# Aggiungere db.properties al .gitignore

db.url=jdbc:postgresql://localhost:5432/hackathon_manager
db.username=postgres
db.password=Alessandro14
db.driver=org.postgresql.Driver

# Connection Pool Settings (opzionali)
db.connection.timeout=30000
db.max.connections=10
